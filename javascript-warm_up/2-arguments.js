#!/usr/bin/node
const args = process.argv.slice(2);

if (argv.length === 0)  {
    console.log('No argument');
}

else if (argv.lengtj === 1) {
    console.log('Argument found');
}

else  {
    console.log('Arguments found');
}
