#!/usr/bin/python3

def update_dictionary(a_dictionary, key, value):
    a_dictionary[key] = value

# dictionaries are permutable i.e you can modify
# or add keys/values as you go, and if it doesn't
# exist, it will create a new one